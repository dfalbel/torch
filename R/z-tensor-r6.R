`torch::Tensor`$set("public", "abs", function() {
  `torch::Tensor`$dispatch(torch_abs_(self$pointer))
})

`torch::Tensor`$set("public", "abs_", function() {
  `torch::Tensor`$dispatch(torch_abs__(self$pointer))
})

`torch::Tensor`$set("public", "acos", function() {
  `torch::Tensor`$dispatch(torch_acos_(self$pointer))
})

`torch::Tensor`$set("public", "acos_", function() {
  `torch::Tensor`$dispatch(torch_acos__(self$pointer))
})

`torch::Tensor`$set("public", "asin", function() {
  `torch::Tensor`$dispatch(torch_asin_(self$pointer))
})

`torch::Tensor`$set("public", "asin_", function() {
  `torch::Tensor`$dispatch(torch_asin__(self$pointer))
})

`torch::Tensor`$set("public", "atan", function() {
  `torch::Tensor`$dispatch(torch_atan_(self$pointer))
})

`torch::Tensor`$set("public", "atan_", function() {
  `torch::Tensor`$dispatch(torch_atan__(self$pointer))
})

`torch::Tensor`$set("public", "bitwise_not", function() {
  `torch::Tensor`$dispatch(torch_bitwise_not_(self$pointer))
})

`torch::Tensor`$set("public", "bitwise_not_", function() {
  `torch::Tensor`$dispatch(torch_bitwise_not__(self$pointer))
})

`torch::Tensor`$set("public", "ceil", function() {
  `torch::Tensor`$dispatch(torch_ceil_(self$pointer))
})

`torch::Tensor`$set("public", "ceil_", function() {
  `torch::Tensor`$dispatch(torch_ceil__(self$pointer))
})

`torch::Tensor`$set("public", "cos", function() {
  `torch::Tensor`$dispatch(torch_cos_(self$pointer))
})

`torch::Tensor`$set("public", "cos_", function() {
  `torch::Tensor`$dispatch(torch_cos__(self$pointer))
})

`torch::Tensor`$set("public", "cosh", function() {
  `torch::Tensor`$dispatch(torch_cosh_(self$pointer))
})

`torch::Tensor`$set("public", "cosh_", function() {
  `torch::Tensor`$dispatch(torch_cosh__(self$pointer))
})

`torch::Tensor`$set("public", "det", function() {
  `torch::Tensor`$dispatch(torch_det_(self$pointer))
})

`torch::Tensor`$set("public", "erf", function() {
  `torch::Tensor`$dispatch(torch_erf_(self$pointer))
})

`torch::Tensor`$set("public", "erf_", function() {
  `torch::Tensor`$dispatch(torch_erf__(self$pointer))
})

`torch::Tensor`$set("public", "erfc", function() {
  `torch::Tensor`$dispatch(torch_erfc_(self$pointer))
})

`torch::Tensor`$set("public", "erfc_", function() {
  `torch::Tensor`$dispatch(torch_erfc__(self$pointer))
})

`torch::Tensor`$set("public", "exp", function() {
  `torch::Tensor`$dispatch(torch_exp_(self$pointer))
})

`torch::Tensor`$set("public", "exp_", function() {
  `torch::Tensor`$dispatch(torch_exp__(self$pointer))
})

`torch::Tensor`$set("public", "expm1", function() {
  `torch::Tensor`$dispatch(torch_expm1_(self$pointer))
})

`torch::Tensor`$set("public", "expm1_", function() {
  `torch::Tensor`$dispatch(torch_expm1__(self$pointer))
})

`torch::Tensor`$set("public", "floor", function() {
  `torch::Tensor`$dispatch(torch_floor_(self$pointer))
})

`torch::Tensor`$set("public", "floor_", function() {
  `torch::Tensor`$dispatch(torch_floor__(self$pointer))
})

`torch::Tensor`$set("public", "frac", function() {
  `torch::Tensor`$dispatch(torch_frac_(self$pointer))
})

`torch::Tensor`$set("public", "frac_", function() {
  `torch::Tensor`$dispatch(torch_frac__(self$pointer))
})

`torch::Tensor`$set("public", "inverse", function() {
  `torch::Tensor`$dispatch(torch_inverse_(self$pointer))
})

`torch::Tensor`$set("public", "is_distributed", function() {
  `torch::Tensor`$dispatch(torch_is_distributed_(self$pointer))
})

`torch::Tensor`$set("public", "is_floating_point", function() {
  `torch::Tensor`$dispatch(torch_is_floating_point_(self$pointer))
})

`torch::Tensor`$set("public", "is_complex", function() {
  `torch::Tensor`$dispatch(torch_is_complex_(self$pointer))
})

`torch::Tensor`$set("public", "is_nonzero", function() {
  `torch::Tensor`$dispatch(torch_is_nonzero_(self$pointer))
})

`torch::Tensor`$set("public", "is_signed", function() {
  `torch::Tensor`$dispatch(torch_is_signed_(self$pointer))
})

`torch::Tensor`$set("public", "log", function() {
  `torch::Tensor`$dispatch(torch_log_(self$pointer))
})

`torch::Tensor`$set("public", "log_", function() {
  `torch::Tensor`$dispatch(torch_log__(self$pointer))
})

`torch::Tensor`$set("public", "log10", function() {
  `torch::Tensor`$dispatch(torch_log10_(self$pointer))
})

`torch::Tensor`$set("public", "log10_", function() {
  `torch::Tensor`$dispatch(torch_log10__(self$pointer))
})

`torch::Tensor`$set("public", "log1p", function() {
  `torch::Tensor`$dispatch(torch_log1p_(self$pointer))
})

`torch::Tensor`$set("public", "log1p_", function() {
  `torch::Tensor`$dispatch(torch_log1p__(self$pointer))
})

`torch::Tensor`$set("public", "log2", function() {
  `torch::Tensor`$dispatch(torch_log2_(self$pointer))
})

`torch::Tensor`$set("public", "log2_", function() {
  `torch::Tensor`$dispatch(torch_log2__(self$pointer))
})

`torch::Tensor`$set("public", "logdet", function() {
  `torch::Tensor`$dispatch(torch_logdet_(self$pointer))
})

`torch::Tensor`$set("public", "numpy_T", function() {
  `torch::Tensor`$dispatch(torch_numpy_T_(self$pointer))
})

`torch::Tensor`$set("public", "pin_memory", function() {
  `torch::Tensor`$dispatch(torch_pin_memory_(self$pointer))
})

`torch::Tensor`$set("public", "reciprocal", function() {
  `torch::Tensor`$dispatch(torch_reciprocal_(self$pointer))
})

`torch::Tensor`$set("public", "reciprocal_", function() {
  `torch::Tensor`$dispatch(torch_reciprocal__(self$pointer))
})

`torch::Tensor`$set("public", "neg", function() {
  `torch::Tensor`$dispatch(torch_neg_(self$pointer))
})

`torch::Tensor`$set("public", "neg_", function() {
  `torch::Tensor`$dispatch(torch_neg__(self$pointer))
})

`torch::Tensor`$set("public", "round", function() {
  `torch::Tensor`$dispatch(torch_round_(self$pointer))
})

`torch::Tensor`$set("public", "round_", function() {
  `torch::Tensor`$dispatch(torch_round__(self$pointer))
})

`torch::Tensor`$set("public", "relu", function() {
  `torch::Tensor`$dispatch(torch_relu_(self$pointer))
})

`torch::Tensor`$set("public", "relu_", function() {
  `torch::Tensor`$dispatch(torch_relu__(self$pointer))
})

`torch::Tensor`$set("public", "rsqrt", function() {
  `torch::Tensor`$dispatch(torch_rsqrt_(self$pointer))
})

`torch::Tensor`$set("public", "rsqrt_", function() {
  `torch::Tensor`$dispatch(torch_rsqrt__(self$pointer))
})

`torch::Tensor`$set("public", "sigmoid", function() {
  `torch::Tensor`$dispatch(torch_sigmoid_(self$pointer))
})

`torch::Tensor`$set("public", "sigmoid_", function() {
  `torch::Tensor`$dispatch(torch_sigmoid__(self$pointer))
})

`torch::Tensor`$set("public", "sin", function() {
  `torch::Tensor`$dispatch(torch_sin_(self$pointer))
})

`torch::Tensor`$set("public", "sin_", function() {
  `torch::Tensor`$dispatch(torch_sin__(self$pointer))
})

`torch::Tensor`$set("public", "sinh", function() {
  `torch::Tensor`$dispatch(torch_sinh_(self$pointer))
})

`torch::Tensor`$set("public", "sinh_", function() {
  `torch::Tensor`$dispatch(torch_sinh__(self$pointer))
})

`torch::Tensor`$set("public", "detach", function() {
  `torch::Tensor`$dispatch(torch_detach_(self$pointer))
})

`torch::Tensor`$set("public", "detach_", function() {
  `torch::Tensor`$dispatch(torch_detach__(self$pointer))
})

`torch::Tensor`$set("public", "slogdet", function() {
  `torch::Tensor`$dispatch(torch_slogdet_(self$pointer))
})

`torch::Tensor`$set("public", "sqrt", function() {
  `torch::Tensor`$dispatch(torch_sqrt_(self$pointer))
})

`torch::Tensor`$set("public", "sqrt_", function() {
  `torch::Tensor`$dispatch(torch_sqrt__(self$pointer))
})

`torch::Tensor`$set("public", "t", function() {
  `torch::Tensor`$dispatch(torch_t_(self$pointer))
})

`torch::Tensor`$set("public", "t_", function() {
  `torch::Tensor`$dispatch(torch_t__(self$pointer))
})

`torch::Tensor`$set("public", "tan", function() {
  `torch::Tensor`$dispatch(torch_tan_(self$pointer))
})

`torch::Tensor`$set("public", "tan_", function() {
  `torch::Tensor`$dispatch(torch_tan__(self$pointer))
})

`torch::Tensor`$set("public", "tanh", function() {
  `torch::Tensor`$dispatch(torch_tanh_(self$pointer))
})

`torch::Tensor`$set("public", "tanh_", function() {
  `torch::Tensor`$dispatch(torch_tanh__(self$pointer))
})

`torch::Tensor`$set("public", "trunc", function() {
  `torch::Tensor`$dispatch(torch_trunc_(self$pointer))
})

`torch::Tensor`$set("public", "trunc_", function() {
  `torch::Tensor`$dispatch(torch_trunc__(self$pointer))
})

`torch::Tensor`$set("public", "clone", function() {
  `torch::Tensor`$dispatch(torch_clone_(self$pointer))
})

`torch::Tensor`$set("public", "zero_", function() {
  `torch::Tensor`$dispatch(torch_zero__(self$pointer))
})

`torch::Tensor`$set("public", "to_dense", function() {
  `torch::Tensor`$dispatch(torch_to_dense_(self$pointer))
})

`torch::Tensor`$set("public", "sparse_dim", function() {
  `torch::Tensor`$dispatch(torch_sparse_dim_(self$pointer))
})

`torch::Tensor`$set("public", "_dimI", function() {
  `torch::Tensor`$dispatch(torch__dimI_(self$pointer))
})

`torch::Tensor`$set("public", "dense_dim", function() {
  `torch::Tensor`$dispatch(torch_dense_dim_(self$pointer))
})

`torch::Tensor`$set("public", "_dimV", function() {
  `torch::Tensor`$dispatch(torch__dimV_(self$pointer))
})

`torch::Tensor`$set("public", "_nnz", function() {
  `torch::Tensor`$dispatch(torch__nnz_(self$pointer))
})

`torch::Tensor`$set("public", "coalesce", function() {
  `torch::Tensor`$dispatch(torch_coalesce_(self$pointer))
})

`torch::Tensor`$set("public", "is_coalesced", function() {
  `torch::Tensor`$dispatch(torch_is_coalesced_(self$pointer))
})

`torch::Tensor`$set("public", "_indices", function() {
  `torch::Tensor`$dispatch(torch__indices_(self$pointer))
})

`torch::Tensor`$set("public", "_values", function() {
  `torch::Tensor`$dispatch(torch__values_(self$pointer))
})

`torch::Tensor`$set("public", "indices", function() {
  `torch::Tensor`$dispatch(torch_indices_(self$pointer))
})

`torch::Tensor`$set("public", "values", function() {
  `torch::Tensor`$dispatch(torch_values_(self$pointer))
})

`torch::Tensor`$set("public", "numel", function() {
  `torch::Tensor`$dispatch(torch_numel_(self$pointer))
})

`torch::Tensor`$set("public", "to_mkldnn", function() {
  `torch::Tensor`$dispatch(torch_to_mkldnn_(self$pointer))
})

`torch::Tensor`$set("public", "dequantize", function() {
  `torch::Tensor`$dispatch(torch_dequantize_(self$pointer))
})

`torch::Tensor`$set("public", "q_scale", function() {
  `torch::Tensor`$dispatch(torch_q_scale_(self$pointer))
})

`torch::Tensor`$set("public", "q_zero_point", function() {
  `torch::Tensor`$dispatch(torch_q_zero_point_(self$pointer))
})

`torch::Tensor`$set("public", "int_repr", function() {
  `torch::Tensor`$dispatch(torch_int_repr_(self$pointer))
})

`torch::Tensor`$set("public", "qscheme", function() {
  `torch::Tensor`$dispatch(torch_qscheme_(self$pointer))
})

`torch::Tensor`$set("public", "item", function() {
  `torch::Tensor`$dispatch(torch_item_(self$pointer))
})

`torch::Tensor`$set("public", "lgamma_", function() {
  `torch::Tensor`$dispatch(torch_lgamma__(self$pointer))
})

`torch::Tensor`$set("public", "digamma_", function() {
  `torch::Tensor`$dispatch(torch_digamma__(self$pointer))
})

`torch::Tensor`$set("public", "erfinv_", function() {
  `torch::Tensor`$dispatch(torch_erfinv__(self$pointer))
})

`torch::Tensor`$set("public", "sign_", function() {
  `torch::Tensor`$dispatch(torch_sign__(self$pointer))
})

`torch::Tensor`$set("public", "trace", function() {
  `torch::Tensor`$dispatch(torch_trace_(self$pointer))
})

`torch::Tensor`$set("public", "nonzero", function() {
  `torch::Tensor`$dispatch(torch_nonzero_(self$pointer))
})

`torch::Tensor`$set("public", "nonzero_numpy", function() {
  `torch::Tensor`$dispatch(torch_nonzero_numpy_(self$pointer))
})

`torch::Tensor`$set("public", "geqrf", function() {
  `torch::Tensor`$dispatch(torch_geqrf_(self$pointer))
})

`torch::Tensor`$set("public", "lgamma", function() {
  `torch::Tensor`$dispatch(torch_lgamma_(self$pointer))
})

`torch::Tensor`$set("public", "digamma", function() {
  `torch::Tensor`$dispatch(torch_digamma_(self$pointer))
})

`torch::Tensor`$set("public", "erfinv", function() {
  `torch::Tensor`$dispatch(torch_erfinv_(self$pointer))
})

`torch::Tensor`$set("public", "sign", function() {
  `torch::Tensor`$dispatch(torch_sign_(self$pointer))
})

`torch::Tensor`$set("public", "alias", function() {
  `torch::Tensor`$dispatch(torch_alias_(self$pointer))
})
